# Database Configuration (Explicit)
# Update these with your local PostgreSQL credentials
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=mini_exchange
DB_SSLMODE=disable

# Security
JWT_SECRET=super-secret-dev-key-change-me-in-prod

# Server
PORT=8080
